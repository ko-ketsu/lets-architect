# Let's Architect! 技術仕様書

コンセプトは [CONCEPT.md](../CONCEPT.md) を参照。本書は実装者・シナリオ執筆者向けの仕様。

## 1. 技術方針

- **ビルドなし**。素の HTML / CSS / JavaScript(ES modules)。npm・外部ライブラリ・CDN・Web フォント禁止(システムフォントを使う)。
- GitHub Pages で `https://<user>.github.io/<repo>/` 配下に配信される。**すべてのパス参照は相対パス**(`./css/style.css` 等)。
- シナリオは `data/` 配下の JSON を `fetch` で読む。
- 対応ブラウザ: モダンブラウザのみ(ES2022 可)。モバイルファースト、レスポンシブ必須。

## 2. ファイル構成

```
index.html            エントリポイント(SPA、画面はJSで切り替え)
css/style.css         全スタイル
js/main.js            起動・画面遷移(ルータ相当)
js/engine.js          ゲームエンジン(純ロジック、DOM禁止 → Nodeでテスト可能に)
js/ui.js              描画(DOM操作はここに集約)
js/storage.js         localStorage ラッパー
data/index.json       シーズン・エピソード一覧
data/episodes/*.json  各エピソード
scripts/smoke-test.mjs Node で engine.js を通しプレイするスモークテスト
```

## 3. データスキーマ

### 3.1 data/index.json

```json
{
  "seasons": [
    { "id": 1, "title": "要件を聞く", "description": "曖昧な要望を要件に翻訳する力を鍛える" }
  ],
  "episodes": [
    {
      "id": "s1e1",
      "file": "episodes/s1e1.json",
      "season": 1,
      "order": 1,
      "title": "「絶対に落ちないシステム」の正体",
      "summary": "1〜2文の紹介文",
      "estimatedMinutes": 8,
      "difficulty": 1
    }
  ]
}
```

- `difficulty`: 1〜3。エピソード選択画面で ★☆☆〜★★★ として表示(シーズン1=1、シーズン2=2 が目安)。

### 3.2 エピソード JSON(data/episodes/*.json)

```json
{
  "id": "s1e1",
  "season": 1,
  "title": "エピソードタイトル",
  "summary": "紹介文",
  "estimatedMinutes": 8,
  "characters": {
    "player":   { "name": "あなた", "role": "player" },
    "senpai":   { "name": "アオイ先輩", "role": "mentor" },
    "customer": { "name": "丸川部長(マルカワ食品)", "role": "customer" }
  },
  "params": { "customer": 50, "quality": 50, "budget": 50, "trust": 50 },
  "start": "intro",
  "nodes": { "...": "..." },
  "glossary": [
    { "term": "SLA", "def": "説明文" }
  ]
}
```

- `characters` のキーが各ノードの `speaker` に対応する。`"narration"` は予約語(地の文)で定義不要。
- `params` は開始値。値は 0〜100 にクランプ。4軸固定:
  - `customer` 顧客満足 / `quality` システム品質 / `budget` 予算・納期 / `trust` 信頼

### 3.3 ノード種別

`nodes` はノードIDをキーとするオブジェクト。種別は `type` で判別。

**scene(会話・地の文)**
```json
{
  "type": "scene",
  "lines": [
    { "speaker": "narration", "text": "..." },
    { "speaker": "senpai", "text": "..." }
  ],
  "effects": { "budget": -25 },
  "next": "次のノードID"
}
```
- `effects` は省略可(シーズン4で導入)。choice の effects と同じ適用規則(PARAM_KEYS のみ・0〜100 クランプ)で、シーンの lines を表示し終えた後に適用する。外部イベントがパラメータを直接動かす演出(揺さぶりイベント)に使う。UI は choice のフィードバックカードと同様の形でパラメータ増減を明示する(アオイのコメントはなし)。

**choice(選択)**
```json
{
  "type": "choice",
  "prompt": "プレイヤーへの問いかけ",
  "options": [
    {
      "text": "選択肢の文(プレイヤーの発言や行動)",
      "effects": { "trust": 10, "budget": -5 },
      "flags": ["asked_impact"],
      "feedback": "選択直後に表示されるアオイ先輩の一言(なぜ良い/まずいか)",
      "next": "次のノードID"
    }
  ]
}
```
- `effects` / `flags` は省略可。フラグはエピソード内でのみ有効。

**next の条件分岐(シーズン4で導入)**

scene の `next` と choice option の `next` は、文字列(無条件遷移)に加えて次の配列形式も許可する:
```json
"next": [
  { "require": { "flags": ["asked_contract"] }, "to": "shake_mitigated" },
  { "default": true, "to": "shake_full_damage" }
]
```
- 評価規則は ending variants と**完全に同じ**(上から順・`require.flags` は全部保持・`require.minTotal` は合計の下限・最後は必ず `"default": true`)。エンジンの評価関数も ending variants と共通化する(engine.js の `resolveConditional`)。
- choice option の条件付き next は、**その選択肢自身の effects / flags を適用した後の state で評価する**(その選択で取った flag が同じ選択の遷移先に効く)。
- 用途: 序盤の flag によって「同じ選択でも帰結が変わる」分岐(ノード複製の爆発を防ぐ)。ending 直前の分岐は従来どおり ending variants を使い、こちらは道中の展開分岐に使う。

**ending(結末分岐)**
```json
{
  "type": "ending",
  "variants": [
    {
      "require": { "flags": ["asked_impact", "fit_sla"], "minTotal": 280 },
      "lines": [ { "speaker": "narration", "text": "..." } ]
    },
    {
      "default": true,
      "lines": [ ... ]
    }
  ],
  "next": "debrief"
}
```
- 上から順に評価し、最初に条件を満たした variant を再生。`require.flags`(全部保持)と `require.minTotal`(4パラメータ合計の下限)は片方だけでも可。最後は必ず `"default": true`。

**debrief(振り返り)**
```json
{
  "type": "debrief",
  "lines": [ { "speaker": "senpai", "text": "締めの会話" } ],
  "points": [
    "実務に持ち帰れる原則を1〜4個、1文ずつ"
  ]
}
```
- debrief の終了でエピソード完了 → リザルト画面へ。

### 3.4 ランク判定(engine.js に実装)

最終パラメータ合計(開始200・実質上限290前後)で判定: **S ≥ 280 / A ≥ 240 / B ≥ 200 / C はそれ未満**。
S は「ほぼ完璧なルート(各エピソード1〜2本)」でのみ到達できるよう、エピソード追加時は全ルートの合計分布を確認して effects を調整すること(最善ルートの合計が 280〜290 になるのが目安)。

## 4. 画面仕様

SPA。`js/main.js` が画面を切り替える。URLハッシュ(`#/episode/s1e1` 等)で状態を持つとリロードにも耐えて良い。

### 4.1 タイトル画面
- ロゴ(テキストで可)「Let's Architect!」+キャッチコピー「設計の judgement は、体験で学べ。」程度
- ボタン: 「はじめる」(→エピソード選択)/「進捗をリセット」(confirm付き)

### 4.2 エピソード選択画面
- シーズンごとにグルーピングしてエピソードカードを一覧表示
- カード: タイトル / summary / 所要時間 / 難易度(★☆☆☆〜★★★★ の4段階スケール、index.json の difficulty)/ クリア済みなら最高ランクのバッジ
- ロックはなし(全エピソードいつでも遊べる)

### 4.3 プレイ画面(ノベルゲーム風)
- ヘッダー: エピソードタイトル+4パラメータのミニメーター(常時表示)
- メッセージエリア: 話者名ラベル+本文。タップ/クリック/Enterで1行ずつ送る
- choice ノード: 選択肢をカード型ボタンで縦に並べる
- 選択直後: **フィードバックカード**(アオイ先輩のコメント+パラメータ増減を `+10 信頼` のように表示)→タップで先へ
- 用語ハイライト: エピソードの `glossary` の `term` が本文に出現したら下線付きで表示し、タップでポップアップ定義を出す
- 中断: プレイ画面から出る場合は confirm(進捗はエピソード単位、途中セーブなしで良い)

### 4.4 リザルト画面
- 総合ランク(S/A/B/C を大きく)+4パラメータのバー表示
- debrief の points を「持ち帰りメモ」としてリスト表示
- ボタン: 「もう一度」「エピソード選択へ」
- S 未達時は周回を促す一言を添える(例: 「S ルートはこの話に1本だけ。ヒアリングの選び方から見直してみよう」)。S 達成時は称賛のみ

## 5. localStorage

キー `lets-architect:v1`。
```json
{ "episodes": { "s1e1": { "cleared": true, "bestRank": "A", "playCount": 2, "lastPlayedAt": "ISO8601" } } }
```
- ランクは今回結果が既存 bestRank より良い場合のみ更新。

## 6. ビジュアル方針

- ダーク基調(紺〜黒)+アクセント1色(シアン系)。ノベルゲーム風の落ち着いた画面。
- システムフォントスタック。本文は 16px 以上、行間広め。
- パラメータメーターは4色を固定割当(例: customer=橙 / quality=緑 / budget=黄 / trust=青)。
- スマホ(375px幅)で崩れないこと。

## 7. スモークテスト(scripts/smoke-test.mjs)

`node scripts/smoke-test.mjs` で実行。engine.js を import し、
1. data/index.json の全エピソード JSON を読み込みスキーマ必須項目を検証
2. 全ノードの `next` / `start` / ending の参照先が存在することを検証
3. 各エピソードを「常に最初の選択肢」「常に最後の選択肢」「常に最長の文の選択肢」で機械的に通しプレイし、必ず debrief に到達しランクが算出されること、**かつランクが S にならないこと**を確認(正解が位置や文の長さで機械的に当てられる状態を検出する。第10章の正解位置・文長ルール参照)
4. エピソードが参照する `image.src` / `portrait` の実ファイルが data/ 配下に存在することを検証
5. index.json の `difficulty` が 1〜4 であることを検証

scene の `effects`・条件付き `next`(3.3)も通しプレイ・総当たりの両方が正しく反映すること(playThrough が state の flags で next を解決する)。スキーマ拡張自体の回帰テストは `scripts/test-s4-schema.mjs`(インメモリの最小エピソードで scene effects / 条件付き next の挙動を assert)。

補助スクリプト `scripts/check-routes.mjs`: 全エピソードの全選択ルートを総当たりし、ランク分布と最善ルートを表示。**各エピソードで S 到達ルートがちょうど1本**・最善ルート合計が 280〜290 であることを assert する。エピソード追加・改稿時に必ず実行する。

## 8. 図解出題の仕様(シーズン2で導入)

シーズン2から、UML・ER図・ネットワーク図(構成図)を使った出題を導入している。方式は**手作りSVG / 図を読んで選択肢で答える形式**(タップ間違い探しやドラッグ&ドロップは対象外)。

### 8.1 スキーマ拡張

scene / choice ノードに任意フィールド `image` を追加できる。図解出題(ER図・構成図など)と情景イラストの両方をこの1つの仕組みで表示する:

```json
{
  "type": "choice",
  "prompt": "このER図の問題点はどこ?",
  "image": { "src": "diagrams/s2e1-er.svg", "caption": "現行システムのER図", "alt": "顧客・注文・商品の3テーブル構成" },
  "options": [ ... ]
}
```

- `src` は data/ からの相対パス。出題図は `data/diagrams/`、情景イラストは `data/scenes/` に置く。
- エンジン: `validateEpisode` で `src` の形式チェック(実ファイル存在チェックは smoke-test 側で行う)。
- UI: メッセージ/選択肢の上に図を表示。`max-width:100%`、横長の図は `overflow-x:auto` のスクロールコンテナに入れる。タップで拡大表示(モーダル)。`alt` はアクセシビリティ用。

### 8.2 SVG制作ガイドライン

- `viewBox` 必須・固定 width/height は付けない(レスポンシブ対応)
- 配色はゲームのダーク基調に合わせる(背景透過、線・文字は明色。css/style.css のアクセント色を参照)
- 文字は `<text>` 要素で(画像化しない)。フォントはシステムフォント任せ
- 外部参照(画像埋め込み・外部CSS)禁止。1ファイル完結

## 9. 立ち絵の仕様(シーズン2で導入)

キャラクターの立ち絵(手描きSVG)を会話に添える。画風の基準は `data/portraits/` の既存ファイル(アオイ通常 / アオイ指導モード / 丸川部長)。新キャラはこれらに合わせて描く。情景イラストの基準サンプルは `data/scenes/meeting-dusk.svg`。

### 9.1 スキーマ拡張

`characters` の各エントリに任意フィールド `portrait` を追加できる。行単位で表情を差し替える場合は line 側の `portrait` が優先される:

```json
"characters": {
  "senpai": { "name": "アオイ先輩", "role": "mentor", "portrait": "portraits/aoi-normal.svg" }
},
"nodes": {
  "x": { "type": "scene", "lines": [
    { "speaker": "senpai", "text": "……それ、確認せずに約束しちゃった?", "portrait": "portraits/aoi-dry.svg" }
  ] }
}
```

- UI: メッセージボックスの横(モバイルは上)に表示し、話者の切り替わりで差し替える。`portrait` 未定義の話者はこれまで通りテキストのみ。
- フィードバックカードは senpai の指導モード(aoi-dry)を表示する。

### 9.2 立ち絵SVG制作ガイドライン(制作時の教訓を含む)

- `viewBox="0 0 240 320"` のバストアップ固定。新キャラは既存3枚を複製して改変するのが最短。
- 背景: `#182238→#0e1520` の縦グラデ+中心の淡い発光。**発光は必ず縁のない radialGradient で描く**(単色・低透明度の楕円は輪郭が見えてアフロ頭に見える事故が実際に起きた)。
- **隣接パーツは必ず重ねる。端の突き合わせ禁止**: 首は襟の奥(服の開始位置より20px以上下)まで伸ばす / 前髪・サイド髪・顔の間に肌色の隙間を作らない。
- プロポーション: 顎から襟までの首の露出は20〜35px(年配・恰幅の良いキャラは短めに)。
- 顔は単純な楕円ではなく顎がすぼまる卵型パス。目=まぶたライン+瞳+ハイライト、鼻とほうれい線は陰影ラインで表現。
- 仕上げに必ずブラウザのスクリーンショットで目視確認してからコミットする(座標ミスは目でしか見つからない)。
- 8.2 の共通ルール(外部参照禁止・1ファイル完結)も適用。

## 10. シナリオ執筆ガイド(エピソード追加時)

- 世界観: プレイヤーは「ライトブリッジ株式会社」の新人。メンターは**アオイ先輩**(頼れる・少し辛口・説教くさくならない)。顧客キャラは各話ごとに設定。
- 1話 = scene と choice の連なりで **choice 3個前後**、プレイ5〜10分(総テキスト量はお手本 s1e1.json と同程度)。
- 選択肢の設計原則(CONCEPT.md 参照): 明らかな不正解を置かない。「若手がやりがちなもっともらしい罠」(確認せず着手 / 過剰設計 / 丸投げ / 銀の弾丸信仰)を混ぜる。
- feedback は「なぜ」を短く。正解の押しつけではなく視点の提供。
- ending は最低3 variant(良/中/悪)。良い結末は「数か月後にこう効いた」まで描くと学習効果が高い。
- 用語は本文で自然に使い、glossary で定義する(1話 4〜7語)。
- **全シーズン共通**: 各話の最終問題は「状況理解チェック」— それまでのヒアリング内容・顧客の事情を踏まえないと間違える選択肢構成にする。気を抜いた回答は A 落ち程度(即 B/C まで落とさない)。
- **正解の位置は設問ごとにばらす**: 最善の選択肢を `options` の先頭(や末尾)に固めない。固まっていると「常に最初を選ぶだけで S」が成立してしまう(シーズン2初稿で実際に起きた)。smoke-test が first/last 機械プレイで S に到達しないことを assert するので、執筆後に必ず実行して確認する。
- **話をまたいで設問パターンを使い回さない**: 同一シーズンの複数話で、問いの型・選択肢の構成・文言を流用しない(特に最終問題)。シーズン3初稿では3話の最終問題がすべて「スコープをどうする?」の同型+同一文言選択肢になり、3話目はパターンで解けてしまった。プレイヤーは3話続けて遊ぶ前提でレビューする。
- **文の長さで正解が分からないようにする**: 正解の選択肢は「行動」を簡潔に述べ、判断の理由はその選択の `feedback` とデブリーフで語る(選ぶ前に選択肢の中で解説を読める状態にしない)。罠の選択肢にも正解と同程度の具体性・文長を与える。初期9話の監査では「正解=最長文」が31問中26問あり、「常に最長を選ぶ」だけで4話 S・4話 A が取れてしまっていた。smoke-test が最長文戦略の S 非到達を assert する。
- **極端な言い回しを罠の目印にしない**: 「とにかく」「全部」「どうせ」「念のため」のような雑さ・極端さを示す語が罠にだけ現れると、内容を読まずに口調だけで罠を弾ける。正解側にも自然な範囲で混ぜるか、罠から間引いて中身のニュアンスで書き分ける。

### 10.1 シーズン2以降の難易度設計(design/SEASON2.md 参照)

- **choice は 4〜5 回**(ヒアリング含む)。全選択肢がどこかの軸にマイナスを含む(無傷の正解を置かない)。
- **正解の根拠は図に置く**: prompt のテキストだけでは選択肢が同格に見え、`image` の図(ER図・シーケンス図・構成図)を読むと根拠が見つかる構造にする。
  - **出題図には「現状の事実」だけを描く**: 結論・警告の注釈、集計結果(往復回数など)、影響の有無を示す色分け・凡例、これから行う変更の予告(「◯◯を新設予定」等)は答えのネタバレになるため描かない。**特定の要素だけの視覚的強調(枠線のアクセント色・太線化)も同罪**(「ここを見ろ」の示唆になる。全要素を同じスタイルで描く)。`alt` テキストも同様。ヒアリングで得る情報(価格が変わる等)も図に書かない。
- **罠選択肢は「別の文脈なら正解」**: 誤りではなく、この案件の前提では裏目に出る正論。デブリーフで「どういう案件ならそれが正解だったか」まで解説する。
- **ヒアリングは「4候補から2つまで」**: 作中の時間制約として提示。質問の答えは情報(flag)として返り、パラメータ加点は控えめに。外れ質問を1つ混ぜる。ノード構造の例:
  - `hearing1`(choice、候補4つ。各 option は flag を記録し `hearing2_<選んだ質問>` へ)
  - `hearing2_*`(choice、残り3候補)→ 回答 scene → 合流ノードへ
- **ending の最良 variant は `require.flags` + `minTotal` の両方を要求**(点が高いだけ・気づきだけでは最良結末に届かない)。
- 完成後 `node scripts/check-routes.mjs` で全ルート総当たりし、S ルートちょうど1本・最善合計 280〜290 を確認してから確定する。

### 10.2 シーズン3の技術選定設計(design/SEASON3.md 参照)

10.1 のルールに加えて、技術選定型の出題(シーズン3)では以下を守る:

- **出題は技術選定型**: 全選択肢が「別の文脈なら正解」の実在する選択肢(VM/コンテナ/サーバーレス等)。罠の中心は「銀の弾丸信仰」(流行・イメージで選ぶと案件の前提で裏目に出る)。
- **配点はプロセスに寄せる**: S と A を分けるのは「選ぶ前に何をしたか」(裏取りの質問・障害シナリオへの気づき=flags)。技術選択そのものは**制約が答えをほぼ強制する案件設定**にする(例: 運用チーム2人・ピークはセール時のみ100倍)。前提が緩いまま選ばせない。
- **判断材料はエピソード内で与えきる**: 選定判断の前に、必要な知識(コールドスタート、可用性の掛け算など)をヒアリング回答・図・アオイ先輩の解説で供給する。事前知識を前提にしない。glossary 4〜7語に収まらないときは題材を削る。
- **図が決定打になる出題を各話1問死守**: 「この構成で◯◯が落ちたら?」(単一障害点・キャッシュ雪崩)は図を読まないと解けない構成にする。
- **概算は補助スキルとして各話1問程度**: 桁と大小関係が分かれば解けるレベル(電卓不要)。正確な式はデブリーフで解説。図には個々の実測値・仕様値のみを描き、合計・計算結果は描かない(10.1 のネタバレ防止ルールの数値版)。
- **商用サービス名は本文で使わない**: 一般名詞(コンテナ基盤・マネージドDB等)で書く。デブリーフの補足で「実務では◯◯などが該当」と触れる程度は可。

### 10.3 シーズン4のトレードオフ設計(design/SEASON4.md 参照)

10.1・10.2 のルールに加えて、揺さぶり型の出題(シーズン4)では以下を守る:

- **揺さぶりイベントは各話1回・中盤に固定**: 全ルートで必ず発生し、scene の `effects` でパラメータを目に見えて削る。序盤の判断は「回避できるか」ではなく「被害の大小と立て直しの選択肢」(条件付き `next`)に効かせる。
- **配点は「捨て方の作法」に置く**: S と A を分けるのは (1) 目的・制約の確認(裏取り flag)(2) 影響の見積り(概算・図読み flag)(3) 明示的な合意(説明問題の選択)の3手順。「何を守るか」自体は、裏取りで発見できる事実が答えをほぼ強制する案件設定にする(10.2 の原則の継承)。
- **罠の型**: 出題側は「全部守ろうとする」「目的でなく作業量で削る」。説明問題側は「黙って削る(事後報告)」「全部持ち帰る(決められない)」「根性で吸収(チームに皺寄せ)」。
- **説明・合意形成の問題を各話1問**: 配点は trust に寄せる。正解の型は「トレードオフを構造で見せ、選択肢+帰結+推奨案を用意して相手に選ばせる」(選択肢だけ並べる丸投げと書き分ける)。
- **揺さぶり後も最善ルートは S(≥280)に立て直せる**量に effects を調整する(check-routes の「S ちょうど1本・最善 280〜290」は不変)。悪いルートは C まで落ちてよいが、失敗 ending の追体験水準(話固有の失敗シーン・一人称の体感)を維持する。
- **3話の骨格共有に注意**: 揺さぶり→再仕分け→説明の構造は共通のため、出題の切り口・罠の中身・説明の状況を話ごとに書き分ける(s4e1 削る対象の選定 / s4e2 品質基準との衝突 / s4e3 複数制約の同時成立)。
- **デブリーフは毎回「良い設計は変化への保険になる」に回収する**(「どうせ変わるなら設計は無駄」と読まれない締めにする)。s4e3 のデブリーフ・最良 ending はゲーム全体のエンディングへの導線を意識して書く。
